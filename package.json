{
  "name": "qts-ui-tests",
  "private": true,
  "description": "Dev-dependency harness for QTS UI tests (Node unit + jsdom functional). The product itself has no Node dependency.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/ui/js/format.test.mjs tests/ui/js/operations.test.mjs tests/ui/js/status.test.mjs tests/ui/js/workstation.test.mjs"
  },
  "devDependencies": {
    "jsdom": "^25.0.1"
  }
}
